// Define a variable called count with an original value of 0 and then it (add to it) on the following line.

let count = 0; // Define count variable 
count++; // Increasing the value 

console.log(count);